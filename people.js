const people = ['yoshi', 'ryu', 'chun-li', 'mario'];
const ages = [20, 25, 30, 35];
console.log(people);

module.exports = {
    peo: people, ag: ages
};//the thing you want to export to module.js as the return value